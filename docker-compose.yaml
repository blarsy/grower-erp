version: "3.9"
services:
  db:
    image: "postgres:latest"
    restart: always
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_PASSWORD=mysecretpassword
  state:
    image: "redis:latest"
    ports:
      - "6379:6379"
